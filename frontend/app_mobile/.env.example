# Local dev: your dev machine's LAN IP — the phone cannot reach localhost.
# Find it with `ipconfig` (IPv4 Address on your Wi-Fi adapter).
EXPO_PUBLIC_API_URL=http://192.168.1.42:7777

# Deployed backend: use the Railway domain instead (no port needed).
# EXPO_PUBLIC_API_URL=https://your-service.up.railway.app
